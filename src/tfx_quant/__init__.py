"""TfxQuant — Windows desktop automated trading system for Yuanta Futures (元大期貨)."""

__version__ = "0.1.0"
"""Kept in sync with `pyproject.toml`'s `[project].version` by hand — logged at
startup (see `desktop/__main__.py`'s `application_start` event) so a support bundle
identifies which build produced it."""
