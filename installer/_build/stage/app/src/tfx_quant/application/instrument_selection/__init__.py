"""Feature 03: instrument/contract selection, switching, and the validation rules
order submission (Feature 06) and startup (StartupSafetyGate) both depend on.

Depends only on `domain` and `application.ports` — no Yuanta/COM specifics live here,
those stay in `infrastructure.yuanta`.
"""
